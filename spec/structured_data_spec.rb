require "spec_helper"

describe StructuredData do
  it "has a version number" do
    expect(StructuredData::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
